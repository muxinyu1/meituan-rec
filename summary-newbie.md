# 首页推荐排序CTR建模 - CatBoost版本迭代分析（小白版）

> 📚 **写在前面**：本文档面向机器学习/推荐算法领域的初学者，会对所有技术术语进行通俗解释。

---

## 🔤 核心概念速查表

在开始之前，让我们先了解一些核心概念：

| 术语 | 通俗解释 |
|-----|---------|
| **CTR (Click-Through Rate)** | 点击率，即"用户看到推荐内容后点击的概率"。CTR建模就是预测用户会不会点击 |
| **CatBoost** | 一种机器学习算法，特别擅长处理"类别型数据"（如城市、性别等），由俄罗斯Yandex公司开发 |
| **特征 (Feature)** | 描述数据的属性。比如描述一个用户："年龄=25，性别=男，城市=北京"，这里年龄、性别、城市就是特征 |
| **特征工程** | 将原始数据加工成模型更容易学习的形式，是机器学习中最关键的步骤之一 |
| **训练集/测试集** | 训练集用来"教"模型学习规律，测试集用来检验模型学得好不好 |
| **交叉验证 (Cross Validation)** | 把训练数据分成N份，轮流用其中1份验证、其他份训练，得到更稳定的评估结果 |
| **AUC** | 模型评价指标，范围0-1，越接近1表示模型越好。0.5相当于随机猜测 |
| **过拟合** | 模型"死记硬背"了训练数据，导致在新数据上表现差。就像考试只背答案，换个题就不会了 |
| **正则化** | 一种防止过拟合的技术，相当于给模型"加约束"，不让它太复杂 |

---

## 📋 目录
1. [V1 基础版概述](#v1-基础版概述)
2. [V2 升级内容](#v2-升级内容v1--v2)
3. [V3 升级内容](#v3-升级内容v2--v3)
4. [V4 升级内容](#v4-升级内容v3--v4)
5. [V4 vs V1 完整总结](#v4-vs-v1-完整总结)

---

## V1 基础版概述

**文件**: `train_catboost.py`

V1 是最基础的版本，实现了一个能跑起来的CTR预测模型。

### 🎯 V1 做了什么？

#### 1. 基础特征处理

**Count Encoding（计数编码）**
> 💡 **通俗解释**：统计某个值在数据中出现了多少次。比如"用户A下了100单，用户B下了2单"，这个次数信息对预测很有帮助——活跃用户的行为规律更容易把握。

```python
# 统计每个用户ID、商品ID等出现的次数
count_features = ['userid', 'itemid', 'geohash', 'cityid', 'loc_cityid']
```

**时间周期编码（sin/cos）**
> 💡 **通俗解释**：用数学方法把"几点钟"转换成模型更容易理解的形式。比如23点和1点虽然数字差很大，但其实很接近（都是深夜），sin/cos编码能表达这种"循环"关系。

```python
df['hour_sin'] = np.sin(2 * np.pi * hour / 24)  # 把小时转成-1到1之间的值
df['hour_cos'] = np.cos(2 * np.pi * hour / 24)
```

#### 2. 模型配置

```python
params = {
    'iterations': 15000,      # 训练15000轮
    'learning_rate': 0.0626,  # 学习率：每轮学多少，太大容易跑偏，太小学得慢
    'depth': 10,              # 树的深度：越深模型越复杂
    'l2_leaf_reg': 3.69,      # 正则化强度：防止过拟合
}
```

#### 3. 10折交叉验证 + 简单平均集成

> 💡 **通俗解释**：把数据分成10份，训练10个模型，最后把10个模型的预测结果取平均。这样比只训练1个模型更稳定。

---

## V2 升级内容（V1 → V2）

**文件**: `train_catboost_v2.py`

### 🆕 新增功能汇总

| 功能 | 一句话说明 | 效果评级 |
|-----|-----------|---------|
| Target Encoding | 把"类别"转换成"该类别的历史点击率" | ⭐⭐⭐⭐⭐ |
| 缺失值标记 | 记录哪些数据是缺失的 | ⭐⭐⭐ |
| 时间特征增强 | 判断是否周末、处于哪个时段 | ⭐⭐⭐ |
| Count对数变换 | 对计数取对数，减少极端值影响 | ⭐⭐ |
| 交叉特征 | 组合两个特征，如"用户+类别" | ⭐⭐⭐⭐ |
| 数值分箱 | 把连续数值分成几个区间 | ⭐⭐ |

### 📖 详细解释

#### 1. Target Encoding（目标编码）⭐⭐⭐⭐⭐

> 💡 **这是什么？** 
> 
> 假设你要预测"用户会不会点击某个类别的商品"。Target Encoding就是：先统计"这个类别的商品，历史上被点击的概率是多少"，然后用这个概率作为新特征。
>
> **举例**：火锅类商品历史点击率是30%，奶茶类是50%，那么：
> - 火锅商品的这个特征值 = 0.30
> - 奶茶商品的这个特征值 = 0.50

> ⚠️ **防泄露处理**：如果直接用全部数据计算，会导致"数据泄露"（用了未来的信息预测过去）。所以V2使用了**K折编码**——计算某条数据的编码时，不使用这条数据本身。

```python
class CTRFeatureEncoder:
    """基于K折的Target Encoding编码器，避免数据泄露"""
    # 使用贝叶斯平滑：如果某类别样本太少，就向整体平均值靠拢
    smooth_ctr = (该类别点击数 + 平滑系数 × 全局点击率) / (该类别总数 + 平滑系数)
```

**为什么效果好？** 直接把"这个类别好不好卖"的信息喂给模型，模型学习负担大大减轻。

---

#### 2. 缺失值标记特征 ⭐⭐⭐

> 💡 **这是什么？**
>
> 数据缺失本身可能包含信息。比如"价格缺失"可能意味着商家没有填写，这类商家可能有特定的行为模式。

```python
# 为9个高缺失率的列创建"是否缺失"的标记
high_missing_cols = ['price', 'online_days', 'distance', ...]
for col in high_missing_cols:
    df[f'{col}_missing'] = df[col].isnull().astype(int)  # 缺失=1，不缺失=0
```

---

#### 3. 时间特征增强 ⭐⭐⭐

> 💡 **这是什么？**
>
> 把时间信息转换成更有业务意义的特征。

```python
# 是否周末（周末消费行为不同）
df['is_weekend'] = df['weekday'].isin([6, 7])

# 时段分类（不同时段点外卖的习惯不同）
# 1=上午, 2=午餐, 3=下午, 4=晚餐, 0=深夜
def get_time_period(hour):
    if 6 <= hour < 11: return 1   # 上午
    if 11 <= hour < 14: return 2  # 午餐高峰
    if 14 <= hour < 18: return 3  # 下午
    if 18 <= hour < 22: return 4  # 晚餐/晚间
    return 0  # 深夜
```

---

#### 4. 交叉特征 ⭐⭐⭐⭐

> 💡 **这是什么？**
>
> 把两个特征组合成一个新特征。单独看"用户A"和"火锅类"没什么特别，但组合起来"用户A×火锅类"可能就代表了"这个用户对火锅的偏好"。

```python
# 城市匹配：商品所在城市是否等于用户所在城市
df['is_same_city'] = (df['cityid'] == df['loc_cityid'])

# 用户-类别交叉：捕捉"这个用户对这个类别的偏好"
df['user_cate1'] = df['userid'] + '_' + df['cate_1']

# 城市-类别交叉：捕捉"这个城市对这个类别的偏好"（如成都人爱火锅）
df['city_cate1'] = df['cityid'] + '_' + df['cate_1']
```

---

#### 5. 数值分箱 ⭐⭐

> 💡 **这是什么？**
>
> 把连续的数值划分成几个区间。比如距离0-20米=近，20-40米=中等，40米以上=远。这样模型更容易学习"距离近=更可能点击"这种规律。

```python
# 距离分箱：分成5个区间
df['distance_bin'] = pd.cut(df['distance'], 
                            bins=[-1, 20, 40, 60, 80, 200], 
                            labels=[0, 1, 2, 3, 4])
```

---

#### 6. 模型参数调整

| 参数 | V1 | V2 | 为什么调整？ |
|-----|----|----|------------|
| 树深度 | 10 | 8 | 特征变多了，需要降低复杂度防止过拟合 |
| 正则化 | 3.69 | 5.0 | 同上，加强约束 |
| 学习率 | 0.063 | 0.05 | 学慢一点，学得更稳 |
| 迭代次数 | 15000 | 10000 | 特征好了，收敛更快 |

---

## V3 升级内容（V2 → V3）

**文件**: `train_catboost_v3.py`

### 🆕 新增功能汇总

| 功能 | 一句话说明 | 效果评级 |
|-----|-----------|---------|
| 更多交叉特征 | 11组用户属性×物品属性的交叉 | ⭐⭐⭐⭐ |
| 对数变换 | 对长尾分布的数值取对数 | ⭐⭐⭐ |
| 相对统计特征 | 计算"相对于平均值的偏差" | ⭐⭐⭐⭐ |
| 更多Target Encoding | 新增价格区间、距离区间的CTR | ⭐⭐⭐ |

### 📖 详细解释

#### 1. 更多交叉特征 ⭐⭐⭐⭐

V3 大幅扩展了交叉特征，从4组增加到11组：

```python
cross_pairs = [
    ('userid', 'cate_1'),   # 用户×一级类别
    ('userid', 'cate_2'),   # 用户×二级类别
    ('gender', 'cate_1'),   # 性别×类别（女性可能更喜欢甜品？）
    ('age', 'cate_1'),      # 年龄×类别（年轻人可能更喜欢奶茶？）
    ('level', 'cate_1'),    # 用户等级×类别
    ('gender', 'dtype'),    # 性别×商品类型
    ('age', 'dtype'),       # 年龄×商品类型
    # ...
]
```

**为什么有效？** 不同人群对不同品类的偏好差异很大，交叉特征能捕捉这种细粒度的偏好。

---

#### 2. 对数变换 ⭐⭐⭐

> 💡 **这是什么？**
>
> 有些数据分布很"长尾"，比如大部分商品价格在20-50元，但有少数奢侈品价格上千。直接用原始值，模型会被极端值干扰。取对数后，数据分布更均匀。

```python
# 对价格取对数：log(50) ≈ 3.9, log(1000) ≈ 6.9
# 原本差20倍，现在只差1.8倍
df['price_log'] = np.log1p(df['price'])  # log1p = log(x+1)，避免log(0)
```

**适用的列**：`distance`, `price`, `item_ave_price`, `user_home_dis`, `user_work_dis`

---

#### 3. 相对统计特征 ⭐⭐⭐⭐

> 💡 **这是什么？**
>
> "50元"贵不贵？这取决于你买的是什么。50元的快餐很贵，50元的正餐很便宜。相对特征就是计算"这个值相对于同类平均值的偏差"。

```python
# 计算每个类别的平均价格
cate1_price_mean = df.groupby('cate_1')['price'].transform('mean')

# 相对价格 = 实际价格 / 类别平均价格
df['price_relative_to_cate1'] = df['price'] / cate1_price_mean
# 如果 = 1.5，说明比同类平均贵50%
# 如果 = 0.8，说明比同类平均便宜20%

# 价格差值 = 实际价格 - 类别平均价格
df['price_diff_cate1'] = df['price'] - cate1_price_mean
```

**为什么有效？** 模型现在能理解"这个商品在同类中是贵还是便宜"，这比绝对价格更有业务意义。

---

## V4 升级内容（V3 → V4）

**文件**: `train_catboost_v4.py`

V4 是最复杂的版本，有大量优化。

### 🆕 新增功能汇总

| 功能 | 一句话说明 | 效果评级 |
|-----|-----------|---------|
| 移除无效特征 | 删掉sin/cos编码（被证明没用） | ⭐⭐ |
| 精细时段分类 | 7个时段（比V3的5个更细） | ⭐⭐ |
| 高峰/用餐时间 | 新增is_rush_hour, is_meal_time | ⭐⭐⭐ |
| 用户/物品曝光统计 | 统计曝光次数、是否新用户等 | ⭐⭐⭐⭐ |
| 排名特征 | 物品在类别/城市内的热度排名 | ⭐⭐⭐⭐ |
| 交叉Target Encoding | 组合特征的点击率编码 | ⭐⭐⭐⭐⭐ |
| 统计聚合特征 | 类别价格的均值/标准差/极值 | ⭐⭐⭐ |
| 加权集成 | 表现好的模型权重更大 | ⭐⭐⭐ |

### 📖 详细解释

#### 1. 移除无效特征 ⭐⭐

> 💡 **为什么删掉sin/cos？**
>
> 在V3的特征重要性分析中，sin/cos编码的重要性为0。这是因为树模型（如CatBoost）很难学习三角函数的周期性，不如直接用"时段分类"这种离散特征。

---

#### 2. 用户/物品曝光统计 ⭐⭐⭐⭐

> 💡 **这是什么？**
>
> 统计每个用户被推荐了多少次（曝光次数），每个商品被推荐了多少次。这能区分"活跃用户/新用户"和"热门商品/冷门商品"。

```python
# 用户曝光次数（这个用户总共看到了多少次推荐）
user_impressions = train_df.groupby('userid').size()

# 是否新用户（只有1次曝光）
df['is_new_user'] = (df['user_impressions'] <= 1)

# 用户活跃度分箱
# 0=只来过1次, 1=来过2次, 2=来过3-5次, 3=来过6-10次, 4=来过10+次
df['user_activity_bin'] = pd.cut(df['user_impressions'], 
                                  bins=[-1, 1, 2, 5, 10, 100],
                                  labels=[0, 1, 2, 3, 4])
```

> ⚠️ **避免数据泄露**：这里只统计"曝光次数"，不统计"点击次数"，因为点击次数包含了标签信息。

---

#### 3. 排名特征 ⭐⭐⭐⭐

> 💡 **这是什么？**
>
> 不仅知道"这个商品被曝光了100次"，还知道"这个商品在同类别中排第几"。排名信息比绝对数值更有意义。

```python
# 物品在类别内的热度排名
# 比如：这个火锅店在所有火锅店中排第5名
df['cate_item_rank'] = df.groupby('cate_1')['item_impressions'].rank(ascending=False)

# 排名百分位（更稳定）
# 比如：这个火锅店超过了90%的火锅店
df['cate_item_rank_pct'] = df.groupby('cate_1')['item_impressions'].rank(pct=True)
```

---

#### 4. 交叉Target Encoding ⭐⭐⭐⭐⭐

> 💡 **这是什么？**
>
> V2的Target Encoding是对单个特征做的（如"火锅类的点击率"）。V4更进一步，对组合特征做编码（如"北京+火锅的点击率"）。

```python
cross_ctr_cols = [
    ['cityid', 'cate_1'],    # "北京的火锅"点击率
    ['gender', 'cate_1'],    # "女性用户看到甜品"的点击率
    ['age', 'cate_1'],       # "25岁用户看到奶茶"的点击率
    ['time_period', 'dtype'], # "午餐时段看到快餐"的点击率
    ['weekday', 'hour'],     # "周五晚8点"的点击率
]
```

**为什么效果最好？** 这是最直接的"历史经验"——"这种组合情况下，用户点击的概率是多少"。

---

#### 5. 统计聚合特征 ⭐⭐⭐

> 💡 **这是什么？**
>
> 不仅计算平均值，还计算标准差（离散程度）、最小值、最大值，提供更丰富的上下文信息。

```python
# 类别内价格的各种统计
cate1_price_stats = train_df.groupby('cate_1')['price'].agg([
    'mean',  # 平均价格
    'std',   # 标准差（价格分散程度）
    'min',   # 最低价格
    'max'    # 最高价格
])
```

**意义**：模型现在能知道"这个类别的价格区间是20-100元，平均50元，标准差15元"，有助于判断某个价格是否正常。

---

#### 6. 加权集成 ⭐⭐⭐

> 💡 **这是什么？**
>
> V1-V3用简单平均：10个模型的预测取平均。V4用加权平均：验证集上表现好的模型，权重更大。

```python
# 计算权重（用softmax，让差异更明显）
scores = [0.82, 0.83, 0.81, ...]  # 各模型的AUC
weights = softmax((scores - min(scores)) * 100)
# 表现好的模型（0.83）权重约0.12
# 表现差的模型（0.81）权重约0.08

# 加权平均
final_prediction = sum(各模型预测 * 对应权重)
```

---

#### 7. 检查点续训（工程优化）

> 💡 **这是什么？**
>
> 训练10折模型很耗时。如果训练到第7折时程序崩溃，V4可以自动跳过已完成的1-7折，从第8折继续训练。

```python
# 每训练完一折，保存模型
model.save_model(f'checkpoint/fold_{i}.cbm')

# 下次运行时，如果检查点存在，直接加载
if os.path.exists(model_path):
    model.load_model(model_path)  # 跳过训练
```

---

## V4 vs V1 完整总结

### 📊 特征数量对比

| 版本 | 特征数量 | 增长 |
|-----|---------|-----|
| V1 | ~30 | - |
| V2 | ~60 | +100% |
| V3 | ~90 | +50% |
| V4 | ~130+ | +44% |

### 🏆 按重要性排序的新增功能

#### 第一梯队：核心提升（⭐⭐⭐⭐⭐）

| 功能 | 版本引入 | 原理 |
|-----|---------|-----|
| Target Encoding | V2 | 把类别转换成历史点击率 |
| 交叉Target Encoding | V4 | 把组合类别转换成历史点击率 |

> 🎯 **为什么最重要？** 这两个功能直接把"历史经验"编码成特征，让模型不需要自己去"发现"规律。

---

#### 第二梯队：重要提升（⭐⭐⭐⭐）

| 功能 | 版本引入 | 原理 |
|-----|---------|-----|
| 交叉特征 | V2/V3 | 组合两个特征，捕捉交互效应 |
| 用户/物品统计 | V4 | 区分新老用户、冷热门商品 |
| 排名特征 | V4 | 物品在同类中的相对位置 |
| 相对统计特征 | V3 | "相对于同类平均贵/便宜" |

---

#### 第三梯队：有效提升（⭐⭐⭐）

| 功能 | 版本引入 | 原理 |
|-----|---------|-----|
| 缺失值标记 | V2 | 缺失本身可能是一种信息 |
| 时间特征增强 | V2/V4 | 周末、时段、高峰期等 |
| 对数变换 | V3 | 处理长尾分布 |
| 统计聚合 | V4 | 提供类别的统计背景信息 |
| 加权集成 | V4 | 好模型权重更大 |

---

#### 第四梯队：边际提升（⭐⭐）

| 功能 | 版本引入 | 原理 |
|-----|---------|-----|
| 数值分箱 | V2 | 连续值离散化 |
| Count对数 | V2 | 计数值取对数 |
| 移除无效特征 | V4 | 减少噪声 |

---

### 🔧 模型参数演变

随着特征越来越丰富，模型需要更强的"约束"来防止过拟合：

| 参数 | V1 | V4 | 变化趋势 | 为什么？ |
|-----|----|----|---------|---------|
| 树深度 | 10 | 8 | ↓ | 特征好了，不需要太深的树 |
| 正则化 | 3.69 | 8.0 | ↑ | 特征多了，需要更强约束 |
| 学习率 | 0.063 | 0.03 | ↓ | 学慢一点，学得更稳 |
| 叶节点最小样本 | 无 | 80 | 新增 | 防止在小样本上过拟合 |

> 📝 **核心思想**：特征越好，模型可以越"简单"；特征越多，正则化需要越"强"。

---

### 🎯 一句话总结

> **V1 → V4 的核心升级思路**：
>
> 1. **引入历史经验**：通过Target Encoding把"这类商品历史点击率"告诉模型
> 2. **捕捉交互效应**：通过交叉特征发现"特定人群+特定商品"的组合规律  
> 3. **提供相对信息**：告诉模型"这个价格在同类中是高还是低"
> 4. **区分冷热门**：通过曝光统计和排名区分新老用户、热门冷门商品
> 5. **加强正则化**：特征变多后，加强约束防止过拟合

---

## 📚 附录：术语索引

| 术语 | 首次出现 | 解释 |
|-----|---------|-----|
| Target Encoding | V2 | 用类别的历史点击率作为特征值 |
| 交叉特征 | V2 | 两个特征的组合，如"用户ID_类别ID" |
| 贝叶斯平滑 | V2 | 让小样本的统计值向整体平均靠拢 |
| 对数变换 | V3 | 用log(x)代替x，压缩数值范围 |
| 相对特征 | V3 | 实际值与平均值的比值或差值 |
| K折交叉验证 | V1 | 把数据分K份，轮流验证 |
| Early Stopping | V1 | 验证集效果不再提升时停止训练 |
| 正则化 | V1 | 给模型加约束，防止过拟合 |
| 集成学习 | V1 | 多个模型投票/平均得到最终结果 |
| 数据泄露 | V2 | 训练时不小心用了测试数据的信息 |
